import React from 'react';

const PlayerTwoAvatar = (props) => {
  return (
    <img alt="player two avatar" src={props.image} className="overlay__avatar-two"></img>
  );
};

export default PlayerTwoAvatar;