import React from 'react';

const PlayerOneAvatar = (props) => {
  return (
    <img alt="player one avatar" src={props.image} className="overlay__avatar-one"></img>
  );
};

export default PlayerOneAvatar;